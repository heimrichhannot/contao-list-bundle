<div class="wrapper" id="{{ wrapperId }}"{{ dataAttributes|default()|raw }}>
    {% if showInitialResults or submitted %}
        {% if showItemCount %}
            <p class="count">{{ itemsFoundText|raw }}</p>
        {% endif %}
        {% if items is not empty %}
            <div class="items">
                <table>
                    {% if header is defined and header is iterable %}
                        <thead>
                        <tr>
                            {% for i, fieldData in header %}
                                <th class="{{ fieldData.class }}">
                                    {% if sortingHeader %}
                                        <a class="{{ fieldData.sortingClass|default }}" href="{{ fieldData.link }}">
                                            {{ fieldData.label }}
                                        </a>
                                    {% else %}
                                        {{ fieldData.label }}
                                    {% endif %}
                                </th>
                            {% endfor %}
                            {% if addDetails|default() %}
                                <th class="actions"></th>
                            {% endif %}
                        </tr>
                        </thead>
                    {% endif %}

                    <tbody>

                    {% for item in items %}
                        {{ item|raw }}
                    {% endfor %}

                    </tbody>
                </table>
            </div>
            {{ pagination|default()|raw }}
        {% elseif showNoItemsText %}
            <p class="no-items">{{ noItemsText|raw }}</p>
        {% endif %}
        {% if addOverview|default %}
            <div class="overview">
                <a href="{{ jumpToOverview }}" role="button">{{ 'huh.list.labels.overview'|trans }}</a>
            </div>
        {% endif %}
    {% endif %}
</div>
